# Screenshot placeholders

Add the following screenshots to this folder to complete the README:

- `dashboard-screenshot.png` — full dashboard with a few jobs on the Kanban
- `naukri-save.png` — Save to ApplyRadar button on a Naukri job page
- `internshala-save.png` — Save to ApplyRadar button on an Internshala page
- `popup-screenshot.png` — extension popup showing stats + quick save button

To take screenshots:
- Windows: Win + Shift + S (snip), or use the browser's built-in screenshot (F12 → three dots → Screenshot)
- Recommended size: ~1200px wide
